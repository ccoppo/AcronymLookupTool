<Window x:Class="AcronymLookup.UI.DebugUserSwitcher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="[DEBUG] Select User to Impersonate"
        Width="420" Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="DEBUG MODE — Select a user to impersonate at startup. Cancel to use your real Windows account."
                   TextWrapping="Wrap"
                   Foreground="DarkRed"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" 
                 x:Name="UserListBox"
                 DisplayMemberPath="DisplayText"
                 Margin="0,0,0,10"
                 MouseDoubleClick="UserListBox_MouseDoubleClick"/>

        <TextBlock Grid.Row="2"
                   x:Name="StatusText"
                   Text=""
                   Foreground="Gray"
                   FontStyle="Italic"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel (use my account)" Width="160" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Impersonate Selected" Width="150" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>