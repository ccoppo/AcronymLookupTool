<Window x:Class="AcronymLookup.UI.PromoteTermWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Promote Term to Project"
        Width="450"
        Height="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F8F9FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Promote Term to Project"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#212529"
                   Margin="0,0,0,20"/>

        <!-- Info -->
        <Border Grid.Row="1"
                Background="#D1ECF1"
                BorderBrush="#BEE5EB"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="ℹ️ Promote to Project Database"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#0C5460"
                           Margin="0,0,0,8"/>
                <TextBlock Name="TermInfoText"
                           Text="Term: API"
                           FontSize="13"
                           Foreground="#0C5460"
                           TextWrapping="Wrap"/>
                <TextBlock Name="PermissionInfoText"
                           Text="This will create a request for approval by project managers."
                           FontSize="12"
                           Foreground="#0C5460"
                           TextWrapping="Wrap"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Reason Input -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Reason (Optional)"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="#495057"
                       Margin="0,0,0,5"/>
            <TextBox Name="ReasonInput"
                     Height="80"
                     FontSize="14"
                     Padding="8,5"
                     BorderBrush="#CED4DA"
                     BorderThickness="1"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Text="This term would be useful for the team"/>
        </StackPanel>

        <!-- Validation Message -->
        <TextBlock Grid.Row="3"
                   Name="ValidationMessage"
                   Text=""
                   FontSize="12"
                   Foreground="#DC3545"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"
                   Visibility="Collapsed"/>

        <!-- Buttons -->
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Name="PromoteButton"
                    Content="Promote to Project"
                    Width="140"
                    Height="36"
                    Background="#FFC107"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="PromoteButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0A800"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Grid.Column="3"
                    Name="CancelButton"
                    Content="Cancel"
                    Width="100"
                    Height="36"
                    Background="#6C757D"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="CancelButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A6268"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>