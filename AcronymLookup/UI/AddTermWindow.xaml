<Window x:Class="AcronymLookup.UI.AddTermWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Term"
        Width="450"
        Height="500"
        MinHeight="400"
        MaxHeight="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F8F9FA">

    <!-- ADD SCROLLVIEWER HERE TO ENABLE SCROLLING -->
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="Add New Term"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#212529"
                       Margin="0,0,0,20"/>

            <!-- Abbreviation Input -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Text="Abbreviation / Acronym *"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#495057"
                           Margin="0,0,0,5"/>
                <TextBox Name="AbbreviationInput"
                         Height="32"
                         FontSize="14"
                         Padding="8,5"
                         BorderBrush="#CED4DA"
                         BorderThickness="1"/>
            </StackPanel>

            <!-- Definition Input -->
            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <TextBlock Text="Definition *"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#495057"
                           Margin="0,0,0,5"/>
                <TextBox Name="DefinitionInput"
                         Height="80"
                         FontSize="14"
                         Padding="8,5"
                         BorderBrush="#CED4DA"
                         BorderThickness="1"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>
            </StackPanel>

            <!-- Category Input -->
            <StackPanel Grid.Row="3" Margin="0,0,0,15">
                <TextBlock Text="Category (Optional)"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#495057"
                           Margin="0,0,0,5"/>
                <TextBox Name="CategoryInput"
                         Height="32"
                         FontSize="14"
                         Padding="8,5"
                         BorderBrush="#CED4DA"
                         BorderThickness="1"/>
            </StackPanel>

            <!-- Notes Input -->
            <StackPanel Grid.Row="4" Margin="0,0,0,15">
                <TextBlock Text="Notes (Optional)"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#495057"
                           Margin="0,0,0,5"/>
                <TextBox Name="NotesInput"
                         Height="80"
                         FontSize="14"
                         Padding="8,5"
                         BorderBrush="#CED4DA"
                         BorderThickness="1"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>
            </StackPanel>

            <!-- Validation Message -->
            <TextBlock Grid.Row="5"
                       Name="ValidationMessage"
                       Text=""
                       FontSize="12"
                       Foreground="#DC3545"
                       TextWrapping="Wrap"
                       Margin="0,0,0,15"
                       Visibility="Collapsed"/>

            <!-- Buttons -->
            <Grid Grid.Row="6" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1"
                        Name="SaveButton"
                        Content="Save Term"
                        Width="100"
                        Height="36"
                        Background="#28A745"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="SaveButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#218838"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="3"
                        Name="CancelButton"
                        Content="Cancel"
                        Width="100"
                        Height="36"
                        Background="#6C757D"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="CancelButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#5A6268"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </ScrollViewer>
</Window>
